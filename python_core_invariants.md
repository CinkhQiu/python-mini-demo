# Python 核心不变量（从头贯彻到尾）

> 本文不是语法速查，而是 **Python 的底层认知规则**。  
> 只要这些规则不动摇，Python 的任何行为都可以被解释。

---

## 1️⃣ 一切皆对象（Everything is an object）

在 Python 中：

- 数字是对象
- 字符串是对象
- 函数是对象
- 类是对象
- 模块是对象
- 类型本身（type）也是对象

**结论：**
> 只要是“存在于运行期的东西”，几乎都是对象。

这是 Python 灵活性的根源。

---

## 2️⃣ 名字只是绑定（Names are bindings）

变量名不是盒子，不存值，只是 **指向对象的标签**。

```python
a = 3
b = a
```

不是拷贝，而是：

```
a ──▶ 3
b ──▶ 3
```

---

## 3️⃣ 操作的是对象，不是名字

Python 语句真正作用的对象是：

> **名字当前指向的那个对象**

```python
lst.append(1)   # 修改对象
lst = []        # 重新绑定名字
```

---

## 4️⃣ 只要有 `=`，一定是重新绑定

这是一条**绝对规则**：

```python
x = x + 1
obj.attr = 3
User = Other
```

全都是：

> **把名字重新绑定到另一个对象**

---

## 5️⃣ 可变 vs 不可变，决定“是否能原地修改”

- **不可变对象**：`int / str / tuple / frozenset`
- **可变对象**：`list / dict / set / 自定义对象`

函数能否修改外部，只取决于：
1. 有没有重新绑定
2. 对象是否可变

---

## 6️⃣ 属性查找顺序是固定的（实例 → 类 → 父类）

```python
u.attr
```

查找顺序：

1. `u.__dict__`
2. `User.__dict__`
3. 父类（MRO）
4. 找不到 → `AttributeError`

---

## 7️⃣ 函数也是对象，调用是特殊协议

```python
f()
```

本质是：

```python
f.__call__()
```

---

## 8️⃣ 类是对象，`class` 是运行期语句

```python
class User:
    pass
```

本质等价于：

```python
User = type("User", bases, namespace)
```

---

## 9️⃣ Python 的复杂性是“后置且可选的”

- 你可以完全不懂反射、元类
- 但框架在大量使用它们

复杂性被框架吸收。

---

## 🔟 Python 的核心不是语法，而是运行期语义

运行期语义（runtime semantics）关心的是：

- 这行代码执行时到底发生了什么

- 对象有没有被改

- 名字有没有被重新绑定

- 查找顺序是什么

- 调用的是谁

👉 这是“发生了什么”

Python 的难点在于：

> **什么时候发生了什么**

---

## 总结（请记住这三条）

> Python 的一切行为，都可以回溯到：
>
> 1. 一切皆对象  
> 2. 名字只是绑定  
> 3. 行为发生在运行期  

---

*This document is intended to be a long-term mental model, not a quick reference.*
